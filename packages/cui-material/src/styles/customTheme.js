import blueDark from '../colors/blueDark'

const customTheme = {
  palette: {
    neutral: {
      // contrastText: '#fff',
      main: '#4b8a68'
    },
    primary: blueDark
  }
}

export default customTheme
